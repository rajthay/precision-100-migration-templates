AMEND EVENT_DATE IS LESS THAN ISSUE EVENT_DATE

SELECT TU.BG_NUM,TU.EVENT_DATE,TA.BG_NUM,TA.EVENT_DATE
FROM TF001_RISK TU
LEFT JOIN TF001_RISK TA ON TA.BG_NUM=TU.BG_NUM AND TA.FUNC_CODE='A'
WHERE TU.FUNC_CODE='U' AND TO_DATE(TU.EVENT_DATE,'DD-MM-YYYY')<TO_DATE(TA.EVENT_DATE,'DD-MM-YYYY')

==============================

DUPLICATE RECORDS

SELECT FUNC_CODE,BG_NUM,EVENT_DATE,COUNT(1) FROM TF002 GROUP BY FUNC_CODE,BG_NUM,EVENT_DATE HAVING COUNT(1)>1

EDIT TF001_RISK WHERE TRIM(BG_NUM)='PTN16-900-001488' ORDER BY TO_DATE(EVENT_DATE,'DD-MM-YYYY')


=============================